<%= render "shared/header" %>

  <div class="new-container">
    <div class="form-booking">

      <div class="adress-box">
        <h1>Je suis garé au : </h1>
        <h2> 📍 <%= @parking.address %> </h2>
      </div>


      <div class="datetime-box">
        <h1>Je souhaite libérer cette place à:</h1>
        <%= simple_form_for @booking do |f| %>
          <%= f.input :available_at, label: false, as: :datetime, html5: true, input_html:{value: 5.minutes.from_now.strftime('%Y-%m-%dT%k:%M')} %>
          <%= f.submit "CHERCHER UN RELAY", class: "new-btn" %>
        <% end %>
      </div>

    </div>
  </div>


<%= render "shared/navbar" %>
